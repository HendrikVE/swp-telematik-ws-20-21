rule "security system"
when
    Item gWindowSensor received update OPEN
then
    if(SecuritySystem.state == ON) {
        sendBroadcastNotification("SECURITY SYSTEM: SIDE DOOR OPEN")

        setMasterVolume(new PercentType(100))
        playSound("alarm.mp3")
    }
end
